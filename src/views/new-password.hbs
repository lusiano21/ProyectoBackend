<h1>Escriba su nueva contraseña 🛅</h1>
{{#if error }}
  <p>{{error}}</p>
{{/if}}
<form  id="form-login">
    <div class="col-md-5">
    <select id="email" name="email" class="form-select">
      <option value="{{payload.email}}">{{payload.email}}</option>
    </select>
    </div>
    <div class="col-md-5">
        <label for="exampleInputPassword" class="form-label">Contraseña</label>
        <input type="password" name="password" id="password" class="form-control" id="exampleInputPassword" aria-describedby="passwordHelp" required>
        <div id="passwordHelp" class="form-text">Mientas mas caracteres tenga la contraseña sera más segura</div>
    </div>
    <div class="col-12 d-flex justify-content-center">
        <div>
            <button type="submit" class="d-inline p-2 btn btn-primary">Subir</button>
        </div>
    </div>
    <button type="submit">Enviar</button>
</form>
<script src="/static/js/new-password.js"></script>
