<div>
      <h1>Escriba su nueva contraseña 🛅</h1>
      {{#if error }}
      <p>{{error}}</p>
      {{/if}}
      <form action="/api/sessions/new-password" method="post" enctype="text/plain">
        <select id="email" name="email">
          <option value="{{payload.email}}">{{payload.email}}</option>
        </select>
        <input type="password" name="password" placeholder="Contraseña">
        <button type="submit">Enviar</button>
      </form>
</div>

