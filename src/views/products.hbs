<h1>Productos</h1>
<section>
<article class="d-flex justify-content-around">
{{#each payload}}
  <div class="card d-flex flex-column align-items-center flex-wrap" style="width: 18rem;">
    <img src="{{this.image}}" class="card-img-top" alt="restaurante">
    <div class="card-body">
      <h5 class="card-title">{{this.name}}</h5>
      {{#each products}}
      <p class="card-text">{{this.id}}</p>
      <p class="card-text">{{this.menu}}</p>
      <p class="card-text">{{this.price}}</p>
      {{/each}}
      <a href="#" class="btn btn-primary">Comprar</a>
    </div>
  </div>
{{/each}}
</article>
</section>
<p>Página actual: {{page}}</p>
{{#if prevLink}}
  <a href="{{prevLink}}" role="button">Anterior</a>
{{/if}}
{{#if nextLink}}
  <a href="{{nextLink}}" role="button">Siguente</a>
{{/if}}